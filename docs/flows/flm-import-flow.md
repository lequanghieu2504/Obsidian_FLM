# FLM import flow

```text
FLM page/network data
→ extension extractor
→ basic source validation
→ versioned transport package
→ desktop import
→ contract validation
→ FLM normalization
→ canonical academic model
→ source storage and affected derived artifacts
```

File export/import is the initial transport. A future localhost transport may implement the same contract without coupling application internals.
