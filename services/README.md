# Future services

There is no backend in the initial phase. Optional future components may live in `services/api`, `services/indexer`, or `services/worker`, but none are created until needed. Local functionality must never depend on them.
