# FLM core

Boundary from raw FLM representations to canonical domain data. Owns raw models, validation, mapping/normalization, source diffs, and schema/version concerns without depending on current UI structure.
