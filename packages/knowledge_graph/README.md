# Knowledge graph

Storage-agnostic nodes, edges, categorized relations, graph construction, validation, queries, traversal, and serialization. Official and inferred relations must remain distinguishable.
